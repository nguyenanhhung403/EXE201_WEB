# Environment Variables
# Copy to .env - NEVER commit .env!

# API Base URL - Local backend
# Production: https://api.yourdomain.com
# NOTE: In Vite, env vars MUST start with VITE_ prefix
VITE_APP_API_URL=http://localhost:5070
